# CLI

## Common Commands

```bash
pco help
pco bootstrap --json
pco references route "debug a failing test"
pco references capsule "debug a failing test"
pco agentic-auto "ship a verified feature safely"
pco validate
pco readiness
```

## npm Gates

See `docs/validation.md` for release validation.

## Error Standard

Errors should include command, failed check, likely cause, and next evidence-producing command.
